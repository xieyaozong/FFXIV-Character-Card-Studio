# Architecture

The assistant loads personal Markdown notes, chunks them, builds a local vector index, retrieves relevant chunks, and formats a cited answer. Screenshot support is isolated under `vlm/`.
