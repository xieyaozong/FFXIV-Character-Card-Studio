__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
.env
.gradio/
.cache/

# Claude Code tooling config (personal, machine-specific)
.claude/

# Local PowerShell runners — personal param wrappers, not product flow.
# The real flow lives in scripts/ (Python CLIs); see docs/ for commands.
*.ps1

# Model weights, LoRAs, and conditioning checkpoints (downloaded locally, never published)
models/*
!models/README.md
!models/model-manifest.example.yaml
loras/
checkpoints/

# Private FFXIV screenshots and generated output (never published)
character/
example/
private_inputs/*
!private_inputs/.gitkeep
outputs/*
!outputs/.gitkeep
# Per-character LoRA datasets and trained weights — private, the maintainer's IP.
datasets/

# Curated FFXIV knowledge data — the maintainer's IP, kept private like model weights.
# Only the *.example.yaml templates and the format docs are published.
content_packs/ffxiv/anatomy_rules.yaml
content_packs/ffxiv/entities.yaml
content_packs/ffxiv/race_signatures.yaml
content_packs/ffxiv/special_features.yaml
content_packs/ffxiv/*.draft.yaml
content_packs/ffxiv/*.npz
content_packs/ffxiv/lore/
content_packs/ffxiv/reference/
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx
.DS_Store
Thumbs.db
