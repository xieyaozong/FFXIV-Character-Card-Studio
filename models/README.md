# Local Models

Store downloaded VLM, diffusion, ControlNet, IP-Adapter, segmentation, and LoRA files outside git. Record model IDs, revisions, licenses, and local paths in a private environment file or local manifest.

Copy the fields from `model-manifest.example.yaml` into a private local manifest after selecting exact revisions. Do not commit downloaded weights or private character LoRAs.
