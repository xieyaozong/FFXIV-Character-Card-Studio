services:
  studio:
    build: .
    ports:
      - "7860:7860"
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./private_inputs:/app/private_inputs
      - ./outputs:/app/outputs
      - ./models:/app/models
